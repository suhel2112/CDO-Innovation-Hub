version: 2

sources:
  - name: raw
    description: "Raw input datasets (seeded for demo)."
    schema: main
    freshness:
      warn_after: {count: 1, period: day}
      error_after: {count: 2, period: day}
    tables:
      - name: raw_orders
        description: "Raw orders (seed)."
        loaded_at_field: loaded_at
      - name: raw_customers
        description: "Raw customers (seed)."
        loaded_at_field: loaded_at
